<!DOCTYPE html>
<html lang="en">
  <head>
    {% include "meta.njk" %}
    {% if hashes.css == "dev" %}
      <link rel="stylesheet" href="/css/style.css" />
    {% else %}
      <link rel="stylesheet" href="/css/style.{{ hashes.css }}.css" />
    {% endif %}
  </head>
  <body class="bg-canvas text-body font-sans antialiased transition-colors duration-300">
    <header class="bg-surface border-b shadow-sm">
      <div class="mx-auto flex max-w-7xl items-center justify-between px-4 py-4 sm:px-6 lg:px-8">
        <h1 class="text-primary text-xl font-bold">11ty Starter</h1>
        <nav class="flex items-center gap-4">
          <a href="/" class="text-muted hover:text-body transition-colors">Home</a>
          <!-- Dark Mode Toggle -->
          {% include "dark-mode-toggle.njk" %}
        </nav>
      </div>
    </header>
    <main class="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8">{{ content | safe }}</main>
    <footer class="bg-surface mt-12 border-t">
      <div class="mx-auto max-w-7xl px-4 py-6 text-center text-sm text-gray-500 dark:text-gray-400">
        &copy; 2026 11ty Starter Kit. Built with 11ty, Tailwind CSS v4, and Alpine.js.
      </div>
    </footer>
    {% if hashes.js == "dev" %}
      <script src="/js/app.js"></script>
    {% else %}
      <script src="/js/app.{{ hashes.js }}.js"></script>
    {% endif %}
  </body>
</html>
