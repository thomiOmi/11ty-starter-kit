<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title | default("11ty Starter Kit") }}</title>
    <link rel="icon" href="/favicon.ico" />

    <!-- Open Graph -->
    <meta property="og:title" content="{{ title | default('11ty Starter Kit') }}" />
    <meta property="og:type" content="website" />
    <meta property="og:image" content="{% ogImage title | default('11ty Starter Kit'), page.url %}" />

    {% if hashes.css == "dev" %}
      <link rel="stylesheet" href="/css/style.css" />
    {% else %}
      <link rel="stylesheet" href="/css/style.{{ hashes.css }}.css" />
    {% endif %}
  </head>
  <body class="bg-gray-50 font-sans text-gray-900 antialiased">
    <header class="bg-white shadow-sm">
      <div class="mx-auto flex max-w-7xl items-center justify-between px-4 py-4 sm:px-6 lg:px-8">
        <h1 class="text-primary text-xl font-bold">11ty Starter</h1>
        <nav>
          <a href="/" class="text-gray-600 hover:text-gray-900">Home</a>
        </nav>
      </div>
    </header>

    <main class="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8">{{ content | safe }}</main>

    <footer class="mt-12 border-t border-gray-200 bg-white">
      <div class="mx-auto max-w-7xl px-4 py-6 text-center text-sm text-gray-500">
        &copy; 2024 11ty Starter Kit. Built with 11ty, Tailwind CSS v4, and Alpine.js.
      </div>
    </footer>

    {% if hashes.js == "dev" %}
      <script src="/js/app.js"></script>
    {% else %}
      <script src="/js/app.{{ hashes.js }}.js"></script>
    {% endif %}
  </body>
</html>
