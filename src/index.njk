---
layout: layout.njk
title: Welcome to 11ty Starter Kit
---

<div class="space-y-8">
    <!-- Hero Section -->
    <section class="text-center py-12">
        <h1 class="text-4xl font-extrabold tracking-tight text-gray-900 sm:text-5xl md:text-6xl">
            Build modern sites with <span class="text-secondary">Speed</span>
        </h1>
        <p class="mt-4 max-w-md mx-auto text-base text-gray-500 sm:text-lg md:mt-5 md:text-xl md:max-w-3xl">
            A lightweight starter kit combining the power of Eleventy, the utility of Tailwind CSS v4, and the interactivity of Alpine.js.
        </p>
    </section>

    <!-- Interactive Component Demo (Alpine.js) -->
    <section class="bg-white rounded-xl shadow-md p-6 max-w-lg mx-auto" x-data="{ count: 0 }">
        <h2 class="text-2xl font-bold mb-4">Alpine.js Interactivity</h2>
        <p class="mb-4 text-gray-600">This simple counter demonstrates Alpine.js integration.</p>

        <div class="flex items-center justify-center space-x-4">
            <button
                @click="count--"
                class="px-4 py-2 bg-red-100 text-red-600 rounded-lg hover:bg-red-200 transition focus:outline-none focus:ring-2 focus:ring-red-500"
            >
                Decrease
            </button>

            <span x-text="count" class="text-3xl font-bold w-12 text-center"></span>

            <button
                @click="count++"
                class="px-4 py-2 bg-green-100 text-green-600 rounded-lg hover:bg-green-200 transition focus:outline-none focus:ring-2 focus:ring-green-500"
            >
                Increase
            </button>
        </div>

        <div class="mt-6 p-4 bg-gray-100 rounded-lg" x-show="count > 5" x-transition>
            <p class="text-center text-primary font-semibold">Wow, you're clicking a lot!</p>
        </div>
    </section>

    <!-- Tailwind v4 Features Demo -->
    <section class="mt-12">
        <h2 class="text-2xl font-bold mb-6 text-center">Tailwind v4 Configuration</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div class="p-6 border border-gray-200 rounded-lg">
                <h3 class="font-bold text-lg mb-2">CSS Variables Theme</h3>
                <p class="text-gray-600 mb-4">
                    The primary color is defined in CSS variables and used here:
                </p>
                <div class="h-12 w-full bg-primary rounded-md flex items-center justify-center text-white font-mono">
                    bg-primary
                </div>
            </div>
             <div class="p-6 border border-gray-200 rounded-lg">
                <h3 class="font-bold text-lg mb-2">CSS First Config</h3>
                <p class="text-gray-600 mb-4">
                    The secondary color is also defined in CSS variables:
                </p>
                <div class="h-12 w-full bg-secondary rounded-md flex items-center justify-center text-white font-mono">
                    bg-secondary
                </div>
            </div>
        </div>
    </section>
</div>
